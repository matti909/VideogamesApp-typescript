import React from 'react';
import Header from '../components/header/Header';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <Header />
      hola
    </div>
  );
};

export default Home;
